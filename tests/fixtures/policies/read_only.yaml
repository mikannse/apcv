metadata:
  name: "Read-Only Policy"
  version: "1.0"
  description: "Filesystem read-only access"
  author: "security-team"

boundaries:
  tool:
    allowed_tools:
      - search_documents
      - get_issue # 故意漏掉 list_repos
    denied_tools: []

  filesystem:
    allowed_paths:
      - /tmp
      - /var/tmp
    denied_paths:
      - /etc
      - /root
    read_only: true

  network:
    network_enabled: false
    allowed_domains: []
    denied_domains: []
